{
  "name": "voicible",
  "version": "2.0.0",
  "description": "Voicible — real-time speech-to-sign-language interpreter powered by real motion-capture ASL data. \"Every voice, made visible.\"",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "node server/index.js",
    "server": "node server/index.js",
    "indexer": ".venv/bin/python3 sign_processor/mocap_indexer.py",
    "whisper-live": ".venv/bin/python3 sign_processor/run_whisper_live.py",
    "client": "npm run dev --prefix client",
    "client:build": "npm run build --prefix client",
    "install:client": "npm install --prefix client",
    "dev": "concurrently -n indexer,whisper-live,server,client -c magenta,yellow,blue,green \"npm run indexer\" \"npm run whisper-live\" \"npm run server\" \"npm run client\""
  },
  "dependencies": {
    "chalk": "^5.3.0",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "fbx2gltf": "^0.9.7",
    "fuse.js": "^7.0.0",
    "microsoft-cognitiveservices-speech-sdk": "^1.50.0",
    "node-fetch": "^3.3.2",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18"
  }
}
